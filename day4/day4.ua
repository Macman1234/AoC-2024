# Experimental!
&fras "input"

# parse input string into array
⊜∘⊸≠@\n

OrthoViews ← {≡⇌. ⍉. ≡⇌. ∘}

OrthoViews

GetDiags ← (
  +1+⇡⟜(↯⟜⇡)⊸⧻
  ∩(/⊂)
  ⍆⟜⍏
  ⊜□⊙⊏
)
⟜(⍚(GetDiags)⊏[0 1])

/+⍚(/+/+⌕"XMAS")
:
⊂⍚⍚(⇌).
/+°□/+⍚⍚(/+⌕"XMAS")
+